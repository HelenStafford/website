$(document).ready(function () {
  window.offer.getState(function (data) {
    var iso_code = 'US';
    var someText = "Select State";

    addCountries($("#id_country"), iso_code);
    update_country_state(someText);

  });
  $('.single-item').slick({
    arrows: false,
    dots: true,
    autoplay: true,
    autoplaySpeed: 2600,
  });
});