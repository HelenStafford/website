///--------- Added By Chanda ---------------
$( document ).ready(function() {
var currentDate = new Date();
var twoDigitMonth=((currentDate.getMonth()+1)>=10)? (currentDate.getMonth()+1) : '0' + (currentDate.getMonth()+1);
var twoDigitDate=((currentDate.getDate())>=10)? (currentDate.getDate()) : '0' + (currentDate.getDate());
var createdDateTo =  twoDigitDate + "." + twoDigitMonth + "." +  currentDate.getFullYear();
var createdDateToDD =   twoDigitMonth + "." + twoDigitDate + "." +  currentDate.getFullYear();
var createdDateToNew =  twoDigitDate + ", " +  currentDate.getFullYear();
$('.get-dd-mm-yy').html(createdDateToDD);
$('.get-mm-dd-yy').html(createdDateTo);

/// DAY NAME
var dayNames = new Array('Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday');
            var nData = new Date();
			var dayName = dayNames[nData.getDay()];
			$('.day-name').html(dayName);
           // alert(dayNames[nData.getDay()]);



/// Month Name
			Date.prototype.monthNames = [
    "January", "February", "March",
    "April", "May", "June",
    "July", "August", "September",
    "October", "November", "December"
];

Date.prototype.getMonthName = function() {
    return this.monthNames[this.getMonth()];
};
Date.prototype.getShortMonthName = function () {
    return this.getMonthName().substr(0, 3);
};

// usage:
var d = new Date();
var fullMonthname = d.getMonthName();
//alert(d.getMonthName());      // "October"
//alert(d.getShortMonthName()); // "Oct"
var getNewDateFormat = 	fullMonthname + " "+createdDateToNew ;
//alert(getNewDateFormat);
$('.newDateFormat').html(getNewDateFormat)	;


//alert(get_dd_mm_yy);

/// CALCULATOR START
$('.calculate-your-potential-income').click(function(){
	//  alert('Hi');
	  var how_many = $('.how-many option:selected').val(); // A
	  var average_money = $('.average-money option:selected').val(); // B
	  var how_many_days = $('.how-many-days option:selected').val();  //
	  var Daily_income = (how_many)*(average_money); //A x B
      var Weekly_income = (how_many)*(average_money)*(how_many_days);  //A x B x C
      var Monthly_income =  ((how_many)*(average_money)*(how_many_days))*4 ;  // (A x B x C) x 4
      var Yearly_income =  Monthly_income*12 ; //(A x B x C) x 12 or (A x B) x 365 or (A x B x C) x 52
	   //alert(Daily_income);
	   //alert(Weekly_income);
	   //alert(Monthly_income);
	   //alert(Yearly_income);
	   $('.daily-income').html(Daily_income);
       $('.weekly-income').html(Weekly_income);
       $('.monthly-income').html(Monthly_income);
       $('.yearly-income').html(Yearly_income);

	});



/// CALCULATOR END
$('.col-sm-8.col-sm-offset-2.riskfree').click(function(){
  // window.location.href='https://ultimatehomeprofits.com/lp-auto-daily/21steps-w4/order.php?aff_id=370440';
})

});



//------ Added By Chanda end  -------------